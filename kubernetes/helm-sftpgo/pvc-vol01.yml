apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sftpgo-vol01
  namespace: sftpgo
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: csi-cephfs-sc
  volumeMode: Filesystem
